namespace( 'seed', function() {
  desc( 'This is the default task.' );
  task( 'default', function( params ) {
    console.log( 'This is the default seed task with params ' + params );
  });
  
});
    
